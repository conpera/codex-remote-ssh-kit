# codex-remote-ssh-kit — agent entrypoint (read in this order)

New here? Top to bottom. Each line says WHY it's here.
1. **CLAUDE.md / AGENTS.md** — house rules + what you MUST NOT do (CONTRACT).
2. **docs/grounding/code-map/** — where everything lives (start at its index). [run the *project-grounding* skill to build this]
3. **docs/grounding/invariants.md** — what must always hold.
4. **docs/grounding/domain/** — domain rules; read the area you're touching.
5. **docs/grounding/decisions/** — why it's built this way (don't re-litigate).
6. **docs/memory/** — lessons & pitfalls (auto-recalled; skim the `pit-*`).
7. **docs/runbooks/** — how to deploy / test / migrate this repo.

Current working state → `docs/memory/session_continue.yaml`.
